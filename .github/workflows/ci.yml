name: Midnight dApp CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [ 20.x, 22.x ]

    steps:
      - name: 🛒 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔍 TypeScript Typecheck & Lint
        run: npm run lint

      - name: 🧪 Run Automated Vitest Test Suite (Circuits, Nullifiers, Transitions)
        run: npm test

      - name: 🏗️ Compile Compact Circuit Bindings & Build Production Frontend
        run: npm run build

      - name: 📊 Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: midnight-dapp-dist-${{ matrix.node-version }}
          path: dist/